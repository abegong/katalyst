+++
title = "Number Range"
weight = 50
aliases = ["/reference/rules/objects/number-range/"]
+++

<!-- GENERATED by cmd/gendocs from internal/checks/registry.go. Do not edit by hand; run `make docs-gen`. -->

## Check type ID

`kind: object_number_range`

## Purpose

Constrain a numeric field to a minimum and/or maximum value.

## Configuration keys

| Field | Required | Default | Meaning |
|---|---|---|---|
| `field` | yes | — | Frontmatter key to check. |
| `min` | no | — | Inclusive lower bound. At least one of `min`/`max` is required. |
| `max` | no | — | Inclusive upper bound. At least one of `min`/`max` is required. |

## Example

```yaml
collections:
  notes:
    path: notes
    checks:
      - kind: object_number_range
        field: year
        min: 1900
        max: 2100
```
