+++
title = "Explanation page template"
weight = 20
draft = true
+++

<!--
TEMPLATE, copy this file into docs/deep-dives/ and fill it in.

This page IS: an understanding-oriented discussion of *why*: the concepts,
the design rationale, the trade-offs, the alternatives considered and
rejected. It is the home for decision rationale (there is no central
decisions log; the "why" lives with its topic here).

This page is NOT: a reference (no exhaustive field tables, link to the
reference page), a tutorial, or a how-to (no step-by-step instructions).
It explains; it does not instruct.

Write in prose. It is fine to discuss what Katalyst does NOT do and why.
-->

# <Topic>

What this page helps the reader understand, in one or two sentences.

## Background

The context a reader needs: the concept, where it sits in the larger model,
what problem it addresses.

## Design rationale

Why it works the way it does. When this choice replaced an earlier approach,
say so and explain why it changed: this is where that history lives, not in
a separate decision log.

## Trade-offs and alternatives

What was given up, and which alternatives were considered and rejected.

## See also

- The reference page for the precise surface (link with `relref` once you
  copy this into `docs/deep-dives/`)
- Related explanation pages
