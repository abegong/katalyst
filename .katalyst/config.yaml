# Dogfooding katalyst on its own Hugo docs (issues #28, #29).
# The single collection and its schema live under collections/ and schemas/;
# see collections/pages.yaml for the rationale.
#
# Schemas are authored as idiomatic JSON Schema (.json); collections use the
# default YAML convention.
schemas:
  format: json
