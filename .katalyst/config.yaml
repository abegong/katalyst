# Dogfooding katalyst on its own Hugo docs (issues #28, #29).
# The single storage instance and its schema live under storage/ and schemas/;
# see storage/local.yaml for the rationale.
#
# Schemas are authored as idiomatic JSON Schema (.json); storage uses the
# default YAML convention.
schemas:
  format: json
