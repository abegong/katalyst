// Package collection is the home for collection-scoped logic within a project,
// mirroring the domain containment Project ⊃ Collection. Today its only member
// is the query subpackage, which holds the filter and sort grammar that
// operations scoped to a single collection are expressed in.
package collection
